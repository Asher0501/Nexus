# Cross-compilation from Windows to Linux x86_64.
# Uses rust-lld (bundled with Rust) for linking and zig cc for compiling C
# dependencies (rusqlite's bundled SQLite).

[target.x86_64-unknown-linux-gnu]
linker = "rust-lld"
