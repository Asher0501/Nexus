{"route":"again","content":"## 架构审查 — Round 2\n\n### 审查范围\n`design/ARCHITECTURE.md` (Jul 12)，交叉验证 `theory/DESIGN_PHILOSOPHY.md`、`theory/NODE_TRANSFER.md`、`design/DECISIONS.md`、`design/NODE_PROTOCOL.md`。\n\n### 状态：Round 1 的 13 项发现均未修复。本次新增 5 项发现（2 高 / 3 中 / 0 低）。合计 18 项：5 高 / 8 中 / 5 低。\n\n---\n\n## Round 1 遗留（状态不变）\n\n所有 13 项发现（AC-1 ~ AC-6, DR-1 ~ DR-5, LG-1 ~ LG-6）从 Round 1 原样保留，不做重复展开。优先修复顺序见末尾合并列表。\n\n---\n\n## Round 2 新增\n\n### 🔴 AC-7：状态图与重试语义矛盾\n\n**位置**：§5.3 mermaid 状态图 vs §5.4 重试机制\n\n§5.3 状态图包含转移 `Failed --> Pending : retry_node()`，暗示任何 Failed 状态都可以重试。\n\n但 §5.4 明确说：\"重试仅对 Timeout 和 SpawnError（子进程启动失败）生效。exit-code 失败（exit_code ≠ 0）不自动重试，直接走 Failed 出边。\"\n\n状态图没有区分 `Failed(spawn_error)` 和 `Failed(exit_code_nonzero)`——前者可重试，后者不可。一个从 `Failed` 到 `Pending` 的无条件转移边在语义上是错的。\n\n**修正方向**：要么拆分 Failed 为 `SpawnFailed`（可重试）和 `ExecutionFailed`（不可重试），要么在状态图注释中标注 \"仅 spawn 失败可走此转移\"。\n\n---\n\n### 🔴 LG-7：exit_code ≠ 0 时的 exit_reason 处理未定义\n\n**位置**：§5.2 事件循环第 466 行注释 vs 第 471-473 行 event_type 判定\n\n注释说 \"exit_code != 0 且 exit_reason 非空：不重试，直接走向下处理\"——暗示存在一条路径：节点失败但携带有效 exit_reason，走正常的下游路由。\n\n但 event_type 判定逻辑是：\n```\nevent_type = if timed_out { Timeout }\n             else if exit_code == 0 { Complete }\n             else { Failed }\n```\n只要 exit_code ≠ 0 就设置 event_type = Failed，与 exit_reason 有无无关。下游边匹配 `EventType::Failed`，不是 `EventType::Complete`。\n\n这意味着一个 exit_code ≠ 0 但 stdout 输出了 `{\"route\":\"approved\",...}` 的节点——它的 exit_reason=\"approved\" 在 handle_event 中被传入，但 event_type=Failed。边匹配时要同时满足 event_type=Failed AND exit_reason=\"approved\"。这在工作流配置中是合法的——但行为语义模糊：是\"节点失败了，但仍然传递了数据\"还是\"失败就是失败，exit_reason 被忽略\"？\n\n**修正方向**：在 §5.4 或 §4.1 中增加 \"Failed 事件 + exit_reason\" 组合的语义说明。明确 exit_reason 在非 Complete 事件中是否参与边匹配。\n\n---\n\n### 🟡 AC-8：NodeParams 不必要的间接层\n\n**位置**：§3.2 `GraphDef` 结构体\n\n`NodeParams` 只有一个字段：\n```rust\npub struct NodeParams {\n    pub process_timeout_secs: u64,\n}\n```\n它被存储在 `GraphDef.params: HashMap<NodeIndex, NodeParams>` 中——独立于 `NodeData`。\n\n但 `NodeData` 也包含 `process_timeout_secs`（第 222 行）。这是同一数据的两个存储位置。如果 `NodeParams` 只包装一个字段，它没有简化任何东西——它只是一个需要额外维护的包装。\n\n如果 `NodeParams` 的设计意图是将\"执行参数\"从\"节点元数据\"中分离，那它应该包含更多字段（retry 配置、并发权重等）来证明自己的存在。目前只包装超时时间，不如直接存在 NodeData 里。\n\n**修正方向**：要么扩展 NodeParams 包含更多执行参数（retry、timeout、concurrency hint），形成一个有意义的参数子集；要么删除 NodeParams，统一用 NodeData 访问。\n\n---\n\n### 🟡 LG-8：Validator 双重描述未对齐\n\n**位置**：§3.3\n\n§3.3 包含两个独立章节：\n- \"Validator — 运行前一次性验证\"（第 267 行），列出 10 项具体检查\n- \"Validator——分层验证框架\"（第 288 行），引入 L1/L2/L3 三层\n\n两个表之间的关系未说明：10 项检查是否全部属于 L1？L2 \"threshold 可达性静态分析\"对应哪几项？L3 \"Petri net analysis\"与现有检查的关系？\n\n读者需要自己推断映射关系。一个统一表（检查项 → 层级）会比两个独立表更清晰。\n\n**修正方向**：合并为一个表，增加\"层级\"列，将 10 项检查显式分配到 L1/L2/L3。\n\n---\n\n### 🟡 DR-6：设计原则的定理推导过于粗粒度\n\n**位置**：§十 设计原则表（第 916-925 行）\n\n7 条原则全部从局部闭包定理的三个推论推导：\n\n| 原则 | 依赖的命题结论 |\n|------|---------------|\n| 1. Syntax/Semantics 分离 | 等价性 |\n| 2. 状态×结果正交分解 | 等价性 |\n| 3. 同质节点 | 等价性 |\n| 4. 边 = 拓扑 + 策略 | 终止性 + 完备性 |\n| 5. 调度拓扑 ≠ 数据拓扑 | 完备性 |\n| 6. 引擎是执行环境 | 等价性 |\n| 7. 事件驱动解耦 | 等价性 |\n\n5/7 原则映射到同一个推论（等价性）。\"等价性\"足够宽泛以至于几乎任何工程选择都能挂上去——这使得映射失去区分度。\n\n这不是说原则错了——原则本身是好的。但\"从定理推导\"的叙述给了读者一种\"这些原则是被证明的必然选择\"的印象，而实际上大多数是独立的工程判断。\n\n**修正方向**：诚实地区分两类原则——(a) 定理直接要求的（原则 4 的阈值 → 终止性）；(b) 定理允许但不是唯一解的工程选择（原则 1, 2, 3, 5, 6, 7）。把\"依赖的命题结论\"改为\"原理来源\"，增加一列\"是定理必然还是设计选择\"。\n\n---\n\n## 交叉验证：ARCHITECTURE.md ↔ 支持文档\n\n### ARCHITECTURE.md ↔ NODE_PROTOCOL.md\n\n| 检查项 | 状态 |\n|--------|------|\n| stdout JSON 格式 `{\"route\":\"...\",\"content\":\"...\"}` | ✅ 一致 |\n| exit_reason 来自 route 字段 | ✅ 一致 |\n| `returns` 传到 extensions | ⚠️ ARCHITECTURE.md §2 NodeDef 有 returns 字段，NODE_PROTOCOL.md §五说 \"extensions.returns\" 传给节点，但 ARCHITECTURE.md 的 NodeContext 结构体（§6.1）的 extensions 是 `HashMap<String, String>`——returns 是 Vec\\<String\\>，类型不匹配 |\n| 运行时 chunk 流 vs 退出后一次性解析 | ⚠️ NODE_PROTOCOL.md §三描述了\"运行时实时 chunk + 退出后一次性解析\"的双层模型，ARCHITECTURE.md §6 完全没有提及实时 chunk 机制 |\n\n### ARCHITECTURE.md ↔ DESIGN_PHILOSOPHY.md\n\n| 检查项 | 状态 |\n|--------|------|\n| 局部闭包定理引用 | ✅ 一致 |\n| 收敛条件 | ✅ 一致 |\n| All/Any 完备性 | ✅ 一致 |\n| 原则数量 | ⚠️ PHILOSOPHY 导出 3 个推论，ARCHITECTURE 映射出 7 条原则——中间推导链缺失（即 DR-6） |\n\n### ARCHITECTURE.md ↔ DECISIONS.md\n\n| 检查项 | 状态 |\n|--------|------|\n| enum dispatch（非 trait object） | ✅ DECISIONS.md 明确，ARCHITECTURE.md §6.2 通过 NodeExecutor enum 体现 |\n| Spec/Status 分离 | ✅ DECISIONS.md 论证所有权适配，ARCHITECTURE.md §5.4 实现 |\n| `from_provider()` 工厂方法 | ❌ DECISIONS.md 未提及，ARCHITECTURE.md §6.2 引用但未定义——仅在注释中出现 |\n\n---\n\n## 合并优先修复顺序（Round 1 + Round 2）\n\n1. **AC-1** — 原则 4 自相矛盾（1 行修正）\n2. **AC-7** — 状态图与重试语义矛盾（NEW，拆分 Failed 或加注释）\n3. **DR-1** — 补重试范围的设计论证（1 段新增）\n4. **AC-2** — 状态机与正交模型对齐\n5. **LG-7** — exit_code ≠ 0 时的 exit_reason 语义（NEW）\n6. **LG-1** — event_count 重置语义\n7. **LG-2** — RoutePolicyDef 运行时集成\n8. **DR-6** — 设计原则推导粗粒度（NEW）\n9. **AC-3** — 统一维度术语\n10. **AC-4** — NodeTransfer 命名\n11. **DR-2** — Snapshot 权衡讨论\n12. **DR-3** — 两张图分离的代价\n13. **AC-8** — NodeParams 扁平化或扩展（NEW）\n14. **LG-8** — Validator 双重描述对齐（NEW）\n15. **LG-3** — DataRouter 时序边界\n16. **AC-5** — returns 字段语义界定\n17. **AC-6** — RoutePolicyDef 扁平化\n18. **LG-4/LG-5/LG-6/DR-4/DR-5** — 小修小补\n"}
