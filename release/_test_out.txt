{"route":"ok","content":"## Design Philosophy: Nexus Engine\n\n### Core Theorem: Local Closure\nGlobal execution trace = iterative closure of each node's local transfer function. The engine is NOT a controller — it is a closure evaluator.\n\n### Three-Layer Architecture\n1. **Syntax Layer** (WorkflowDef): JSON DAG — nodes, edges, dataflows\n2. **Semantics Layer** (GraphDef): Internal graph with edge decomposition (h_e + g_e)\n3. **Runtime Layer** (Engine): Scheduler + DataRouter + NodeShell\n\n### h_e + g_e Orthogonal Decomposition\nEvery edge decomposes into:\n- h_e (branch matching): Pure function — event type + exit_reason filter + threshold counter. No triggered state.\n- g_e (strategy aggregation): Any = immediate enqueue; All = fan_in_pending → 0 then enqueue.\n\n### Key Design Decisions\n- Edges and dataflows are independent graphs\n- Nodes communicate via stdin/stdout JSON protocol\n- No triggered state in edges (unlike traditional workflow engines)\n- Cycle support via threshold + route_policy (not special-case loop constructs)\n- Convergence via watchdog (30s idle → force-terminate pending nodes)\n\n### Questions for Review\n- Is the h_e/g_e decomposition over-engineered?\n- Should dataflows and scheduling edges be unified?\n- Is the JSON stdout protocol too rigid for real-world nodes?"}
